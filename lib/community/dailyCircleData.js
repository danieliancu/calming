import {
  defaultDailyCircleGroup,
  groupInfo as defaultGroupInfo,
  rawDialogues as defaultRawDialogues,
  roleLabels,
} from "./communityData";

export const groupInfo = defaultGroupInfo;
export const rawDialogues = defaultRawDialogues;
export { roleLabels };
export default defaultDailyCircleGroup;
